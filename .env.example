# Loki Logging runtime environment
# Copy to .env and edit for your environment.
# Do not commit .env — it contains secrets.

# -----------------------------------------------------------------------------
# Project
# -----------------------------------------------------------------------------
COMPOSE_PROJECT_NAME=logging

# -----------------------------------------------------------------------------
# Published Endpoints
# -----------------------------------------------------------------------------
GRAFANA_HOST=0.0.0.0
GRAFANA_PORT=9001
PROM_HOST=0.0.0.0
PROM_PORT=9004
LOKI_PUBLISH=0
LOKI_HOST=127.0.0.1
LOKI_PORT=9002

# -----------------------------------------------------------------------------
# Grafana Credentials and Security
# Generate real values: openssl rand -base64 24 (password), openssl rand -hex 32 (keys)
#
# Dual variable sets explanation:
# - GRAFANA_* vars are used by Grafana provisioning configs (datasource.yml, etc.)
#   via docker-compose env_file substitution in YAML templates.
# - GF_* vars are Grafana's native environment variable override format.
#   GF_SECURITY_ADMIN_USER etc. override grafana.ini settings at runtime.
# Both sets must match. Keep them in sync.
# -----------------------------------------------------------------------------
GRAFANA_ADMIN_USER=admin
GRAFANA_ADMIN_PASSWORD=CHANGE_ME
GRAFANA_SECRET_KEY=CHANGE_ME
GF_SECURITY_ADMIN_USER=admin
GF_SECURITY_ADMIN_PASSWORD=CHANGE_ME
GF_SECURITY_SECRET_KEY=CHANGE_ME

# -----------------------------------------------------------------------------
# Host Path Mappings
# Adjust paths for your system. USER_HOME should be your home directory.
# -----------------------------------------------------------------------------
HOST_HOME=/home
HOST_ROOTFS=/
HOST_VAR_RUN=/var/run
HOST_SYS=/sys
HOST_VAR_LIB_DOCKER=/var/lib/docker
HOST_KMSG=/dev/kmsg
HOST_DOCKER_SOCK=/var/run/docker.sock
HOST_RUN_LOG_JOURNAL=/run/log/journal
HOST_VAR_LOG_JOURNAL=/var/log/journal
HOST_LOGS=/home/YOUR_USER/_logs
HOST_TELEMETRY=/home/YOUR_USER/_telemetry
HOST_VLLM=/home/YOUR_USER/apps/vLLM

# -----------------------------------------------------------------------------
# Container Images and Retention
# -----------------------------------------------------------------------------
GRAFANA_IMAGE=grafana/grafana:11.5.2
LOKI_IMAGE=grafana/loki:3.0.0
PROMETHEUS_IMAGE=prom/prometheus:v2.52.0
NODE_EXPORTER_IMAGE=prom/node-exporter:v1.8.1
CADVISOR_IMAGE=gcr.io/cadvisor/cadvisor:v0.49.1
ALLOY_IMAGE=grafana/alloy:v1.2.1
PROM_RETENTION_TIME=15d

