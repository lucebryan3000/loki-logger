# Loki Logging env (example) — safe to commit

# Ports (host bindings)
GRAFANA_HOST=127.0.0.1
GRAFANA_PORT=9001
PROM_HOST=127.0.0.1
PROM_PORT=9004
# Optional Loki host publish (default off)
LOKI_PUBLISH=0
LOKI_HOST=127.0.0.1
LOKI_PORT=9002

# Compose project/name (if your runner doesn't already set it)
COMPOSE_PROJECT_NAME=infra_observability

# Host bind roots (avoid hard-coded paths in compose)
HOST_HOME=/home
HOST_LUCE_HOME=/home/luce
HOST_LOGS=/home/luce/_logs
HOST_TELEMETRY=/home/luce/_telemetry
HOST_VLLM=/home/luce/apps/vLLM

# Grafana admin (set real values in infra/logging/.env)
GRAFANA_ADMIN_USER=admin
GRAFANA_ADMIN_PASSWORD=CHANGE_ME
GRAFANA_SECRET_KEY=CHANGE_ME

# Telemetry service settings
TELEMETRY_INTERVAL_SEC=10
